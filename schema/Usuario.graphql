type Perfil {
    id: Int
    nome: String
}

enum UsuarioStatus {
    ATIVO
    INATIVO
    BLOQUEADO
}

type Usuario {
    id: ID
    nome: String!
    idade: Int!
    email: String!
    salario: Float
    vip: Boolean
    blabla: String,
    perfil: Perfil
    status: UsuarioStatus
}